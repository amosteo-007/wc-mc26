-- Moved. The canonical Postgres schema lives at data/schema/schema.sql
-- (loaded by docker-compose into the postgres init dir). This file is kept
-- only as a pointer to avoid two copies drifting out of sync.
\i data/schema/schema.sql
