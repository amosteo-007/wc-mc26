# Secrets — never commit real credentials (.env.example is the committed template)
.env
.env.local
.env.*.local
# Claude Code harness helper — contains a DeepSeek token; never commit
set-ds-env.ps1

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Node / Next.js
node_modules/
web/.next/
web/out/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
